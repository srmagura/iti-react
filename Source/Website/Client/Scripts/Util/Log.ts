//import * as Url from 'Url';

//export class Log {
//    static async info(message: string) {
//        return await fetch(Url.get_Log_Info(), { body: message, method: 'post' })
//    }

//    static async warning(message: string) {
//        return await fetch(Url.get_Log_Warning(), { body: message, method: 'post' })
//    }

//    static async error(message: string) {
//        return await fetch(Url.get_Log_Error(), { body: message, method: 'post' })
//    }
//}
